#include "main.h"

/**
 * actual_prime - calculated if a number is a prime recursively.
 * @n: evaluate to a number
 *
 * Return: 1 if n is a prime number.
 */
int actual_prime(int n, int i);
{
	if (n <= 1)
		return (0)
	return (actual_prime(n, n - 1));
}
