#include "main.h"

/**
 * _sqrt_recursion - Returns square root.
 * @n: calculation of a square root.
 *
 * Return: Square root result
 */
int _sqrt_recursion(int n)
{
	if (n < 0)
		return (-1)
	return (actual_sqrt_recursion(n, 0));
}
